apiVersion: v1
kind: ConfigMap
metadata:
  name: redis
  labels:
    app: redis
data:
  redis.conf: |
    appendonly yes
    appendfsync everysec
    aof-use-rdb-preamble yes
    dir /var/lib/redis
    port 6379
    requirepass Conran2017